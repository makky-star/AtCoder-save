#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0;i < (n); ++i)
using ll = long long;

const double pi  = 3.1415;

int main(){
    double a,b,h,m;
    cin >> a >> b >> h >> m;
    double rad;
    double c;

    rad = abs((pi/6.0)*h + (pi/30.0)*m) - 
    cout << cos(rad) << endl;
    
}